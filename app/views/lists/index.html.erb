


</div>
  <div class="container">

  <h1>Lists</h1>

  <% @lists.each do |list| %>
  <%= list.name %>
  <%= link_to "Explore", list_path(list)  %>
  <% end %>
  <div>
    <%= link_to "Create a new list", new_list_path %>
  </div>
</div>
