</div>
  <div class="container">
    <div class="text-center">
      <h1>Lists</h1>
   </div>
<div class="cards">
  <% @lists.each do |list| %>
    <div class="card-category d-flex flex-column mt-5" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)),
          url('<%= cl_image_path list.photo.key, height: 300, width: 400, crop: :fill%>')">
      <h2><%= list.name %></h2>
      <div class="btn btn-light text-white">
        <%= link_to "Explore", list_path(list)  %>
      </div>
    </div>
  <% end %>
</div>
<div>
  <%= link_to "Create a new list", new_list_path, class:"btn btn-primary mt-5"%>
</div>
